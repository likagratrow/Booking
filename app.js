const SHEET_ID='1FcetqNVvXNI78h0mcQdEJBEVXzkHcgaddFrCn2VOugk';
const SHEET_URL=`https://docs.google.com/spreadsheets/d/${SHEET_ID}/gviz/tq?tqx=out:json&sheet=${encodeURIComponent('Активности')}`;
const EVENTS_SHEET_URL=`https://docs.google.com/spreadsheets/d/${SHEET_ID}/gviz/tqx?out:json&sheet=${encodeURIComponent('Ивенты')}`;
const tg=window.Telegram?.WebApp;if(tg){tg.ready();tg.expand();tg.setBackgroundColor?.('bg_color');}
const user=tg?.initDataUnsafe?.user,hello=document.getElementById('hello');if(user)hello.textContent=`Здравствуйте, ${user.first_name||'гость'}!`;
const activitiesContainer=document.getElementById('activities'),calendar=document.getElementById('calendar'),message=document.getElementById('message'),activityModal=document.getElementById('activity-modal'),activityTitle=document.getElementById('activity-title'),activityDescription=document.getElementById('activity-description'),activityImageWrap=document.getElementById('activity-image-wrap'),activityImage=document.getElementById('activity-image'),modalClose=document.getElementById('modal-close'),modalOk=document.getElementById('modal-ok'),eventsModal=document.getElementById('events-modal'),eventsTitle=document.getElementById('events-title'),eventsList=document.getElementById('events-list'),eventsClose=document.getElementById('events-close');
const fallbackActivities=[{key:'diogen',title:'Диоген',description:'Можно прийти в мастерскую и провести время в своём ритме: поработать над чем-то своим, потискать кожу, попить чаю, посидеть в тишине или вообще ничего не делать.',image:''},{key:'masterclass',title:'Мастер-класс',description:'Вы выбираете изделие и приходите делать его вместе с мастером. Мастер-класс проходит в заданное время и длится столько, сколько указано в его описании.',image:''},{key:'order',title:'Обсудить заказ',description:'Если вы хотите сделать изделие на заказ, можно прийти в мастерскую лично: обсудить задумку, материалы, размеры, детали и все нюансы будущей вещи.',image:''}];let activities=fallbackActivities,events=[];
const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&#039;');
function parse(t){const a=t.indexOf('{'),b=t.lastIndexOf('}');if(a<0||b<=a)throw Error('Google Таблица не вернула данные.');return JSON.parse(t.slice(a,b+1));}function cell(c,i,f=''){return c[i]?.v??f;}function norm(v){return String(v||'').trim().toLowerCase();}
async function loadActivities(){activitiesContainer.innerHTML='<div class="loading">Загрузка активностей...</div>';try{const r=await fetch(SHEET_URL,{cache:'no-store'});const j=parse(await r.text());activities=j.table.rows.slice(1).map((x,i)=>{const c=x.c||[];return{key:String(cell(c,0,`activity-${i}`)).trim().toLowerCase(),title:String(cell(c,1,'Без названия')).trim(),description:String(cell(c,2,'')).trim(),image:String(cell(c,3,'')).trim()};}).filter(x=>x.key&&x.title);renderActivities();}catch(e){activitiesContainer.innerHTML=`<div class="load-error">Не удалось загрузить активности: ${esc(e.message)}</div>`;}}
async function loadEvents(){try{const r=await fetch(EVENTS_SHEET_URL,{cache:'no-store'});const j=parse(await r.text());const rows=j.table.rows||[];const headers=(rows[0]?.c||[]).map(c=>String(c?.v??'').trim().toLowerCase());const col=(...names)=>{const i=headers.findIndex(h=>names.includes(h));return i>=0?i:null;};const ai=col('activity','активность'),ni=col('name','title','название','имя'),di=col('description','описание'),pi=col('price','цена'),dui=col('duration','длительность'),agi=col('age','возраст'),ci=col('complexity','сложность'),ii=col('image','картинка','изображение','фото');events=rows.slice(1).map((x,i)=>{const c=x.c||[];return{activity:String(cell(c,ai??0,'')).trim(),name:String(cell(c,ni??1,'Без названия')).trim(),description:String(cell(c,di??2,'')).trim(),price:String(cell(c,pi??3,'')).trim(),duration:String(cell(c,dui??4,'')).trim(),age:String(cell(c,agi??5,'')).trim(),complexity:String(cell(c,ci??6,'')).trim(),image:String(cell(c,ii??7,'')).trim(),index:i};}).filter(x=>x.activity&&x.name);}catch(e){console.error(e);events=[];}}
function renderActivities(){activitiesContainer.innerHTML=activities.map(a=>`<button class="choice" type="button" data-action="${esc(a.key)}"><span class="choice-icon">${a.key==='diogen'?'◌':a.key==='masterclass'?'✦':'✎'}</span><span><strong>${esc(a.title)}</strong><small>${esc(a.description)}</small></span><span class="choice-calendar"><span class="choice-calendar-icon">▣</span><span class="choice-calendar-arrow">↓</span></span></button>`).join('');activitiesContainer.querySelectorAll('.choice').forEach(b=>b.addEventListener('click',e=>e.target.closest('.choice-calendar')?openEventChooser(b.dataset.action):openActivity(b.dataset.action)));}
function openActivity(k){const a=activities.find(x=>x.key===k);if(!a)return;activityTitle.textContent=a.title;activityDescription.textContent=a.description;if(a.image){activityImage.src=a.image;activityImage.alt=a.title;activityImageWrap.classList.remove('hidden');}else activityImageWrap.classList.add('hidden');activityModal.classList.remove('hidden');}
function stars(v){const n=Math.max(0,Math.min(5,parseInt(v,10)||0));return[1,2,3,4,5].map(i=>`<span class="complexity-star${i>5-n?' filled':''}">★</span>`).join('');}
function openEventChooser(k){const key=norm(k);if(key==='order'){closeEvents();goToCalendar('order');return;}const a=activities.find(x=>norm(x.key)===key);if(!a)return;const list=events.filter(x=>norm(x.activity)===key);eventsTitle.textContent=a.title;eventsList.innerHTML=list.length?list.map((e,i)=>`<article class="event-card"><div class="event-image-wrap ${e.image?'':'empty'}">${e.image?`<img class="event-image" src="${esc(e.image)}" alt="${esc(e.name)}">`:'Фото пока нет'}<div class="event-overlay"><h3>${esc(e.name)}</h3><strong class="event-price">${esc(e.price||'—')} р</strong><div class="event-age"><span>Возраст</span><strong>${esc(e.age||'—')}</strong></div><div class="event-complexity"><span>Сложность</span><strong class="complexity-stars">${stars(e.complexity)}</strong></div><button class="event-book" type="button" data-i="${i}">Записаться</button></div></div><div class="event-body"><div class="event-meta"><div class="event-meta-row"><span>Длительность</span><strong>${esc(e.duration||'—')}</strong></div></div><p class="event-description">${esc(e.description)}</p></div></article>`).join(''):'<div class="events-empty">Пока нет доступных вариантов.</div>';eventsList.querySelectorAll('.event-book').forEach(b=>b.addEventListener('click',()=>{const e=list[+b.dataset.i];closeEvents();goToCalendar(norm(e.activity),e.name);}));eventsModal.classList.remove('hidden');}
function closeEvents(){eventsModal.classList.add('hidden');}
const demoDays=[{offset:1,slots:[['10:00–11:00','free','свободно'],['11:00–12:00','free','свободно'],['12:00–13:00','dio','Диоген · 1/2'],['13:00–14:00','dio','Диоген · 1/2'],['15:00–16:00','closed','закрыто'],['16:00–17:00','free','свободно']]},{offset:2,slots:[['10:00–11:00','free','свободно'],['11:00–12:00','mk','МК · 3/5'],['12:00–13:00','mk','МК · 3/5'],['13:00–14:00','mk','МК · 3/5'],['15:00–16:00','free','свободно'],['16:00–17:00','free','свободно']]},{offset:3,slots:[['10:00–11:00','closed','закрыто'],['11:00–12:00','free','свободно'],['12:00–13:00','free','свободно'],['13:00–14:00','dio','Диоген · 2/2'],['14:00–15:00','dio','Диоген · 2/2'],['16:00–17:00','free','свободно']]}];
function renderCalendar(){calendar.innerHTML=demoDays.map(d=>{const x=new Date();x.setHours(12,0,0,0);x.setDate(x.getDate()+d.offset);return`<article class="day"><div class="day-title"><span>${x.toLocaleDateString('ru-RU',{day:'numeric',month:'long',weekday:'long'})}</span></div><div class="slots">${d.slots.map(s=>`<button class="slot ${s[1]}" type="button" data-state="${s[1]}" data-time="${s[0]}"><strong>${s[0]}</strong><small>${s[2]}</small></button>`).join('')}</div></article>`;}).join('');document.querySelectorAll('.slot').forEach(b=>b.addEventListener('click',()=>showMessage(b.dataset.state==='closed'?'Это время закрыто. Выберите другой час.':`Вы выбрали ${b.dataset.time}. Пока это демонстрационный календарь — запись ещё не отправляется.`)));}
function goToCalendar(type,name=''){document.querySelectorAll('.slot.highlighted').forEach(x=>x.classList.remove('highlighted'));document.querySelectorAll(type==='masterclass'?'.slot.mk':'.slot.free').forEach(x=>x.classList.add('highlighted'));showMessage(type==='masterclass'&&name?`Подсвечены места для «${name}» и существующие МК.`:type==='order'?'Подсвечены свободные часы для обсуждения заказа.':'Подсвечены все свободные часы.');setTimeout(()=>document.querySelector('.calendar-card')?.scrollIntoView({behavior:'smooth',block:'start'}),40);}
function showMessage(t){message.textContent=t;message.classList.remove('hidden');message.scrollIntoView({behavior:'smooth',block:'nearest'});}function closeActivity(){activityModal.classList.add('hidden');}
modalClose.addEventListener('click',closeActivity);modalOk.addEventListener('click',closeActivity);activityModal.addEventListener('click',e=>{if(e.target===activityModal)closeActivity();});eventsClose.addEventListener('click',closeEvents);eventsModal.addEventListener('click',e=>{if(e.target===eventsModal)closeEvents();});renderCalendar();Promise.all([loadActivities(),loadEvents()]);